#!/bin/bash

FOLDERNAME="xmodmap-1.0.9"
FILENAME="xmodmap-1.0.9.tar.bz2"

# Xorg Application

# Change directory
cd /src/pkg/standard/${FOLDERNAME}

# Install
./configure $XORG_CONFIG
make
make install